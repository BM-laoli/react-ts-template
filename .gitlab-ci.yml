stages: # 分段
  - build

cache: # 缓存
  paths:
    - node_modules
    - build

package-job:
  tags:
    - sss
  stage: build
  image: node:14-buster  #拉取node镜像
  script:
  - npm install -g nrm
  - npm install 
  - npm run build
  artifacts:  #这个是为了能在下个作业中获取到这个dist
    paths:
    - build

